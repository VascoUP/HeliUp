package com.av.game.graphics;

import com.av.game.logic.object.building.Building;
import com.badlogic.gdx.graphics.Texture;

public class BuildingSprite extends ObjectSprite {
    public BuildingSprite(Building gameObject, Texture texture) {
        super(gameObject, texture);
    }
}
